

# Light Brand-Themed Active State for Stats Filter Buttons

Change the active filter button styling from solid `bg-primary` (too dark) to a light brand tint.

## Changes

### 1. `src/pages/vendor/VendorSeats.tsx` (~line 848)
- Change active class to: `bg-primary/15 text-primary ring-1 ring-inset ring-primary/30 rounded-md`

### 2. `src/pages/admin/HostelBedMap.tsx` (~line 1215)
- Same change: `bg-primary/15 text-primary ring-1 ring-inset ring-primary/30 rounded-md`

Light blue-tinted background with primary text color — visible but not overpowering.

